// Matches the modeled monitor surface (2.5503 : 1.9781) while keeping the
// desktop's coordinates in readable CSS pixels.
export const MONITOR_WIDTH = 1280;
export const MONITOR_HEIGHT = 992;

export const TASKBAR_HEIGHT = 72;
export const DESKTOP_WORKSPACE_HEIGHT = MONITOR_HEIGHT - TASKBAR_HEIGHT;
